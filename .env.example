# palimpsest-stack — environment
#
# Copy to .env on the target machine and fill in. .env is gitignored and must
# stay that way (contract §6). Nothing in this file is read at build time; it
# is interpolated into compose.yaml at `docker compose up`.
#
#   cp .env.example .env && chmod 600 .env && $EDITOR .env
#
# Most of this stack's secrets are NOT here — they are entered in each app's
# web UI after first start. See NOTES.md § "Secrets you supply by hand".


# ---------------------------------------------------------------------------
# Bind addresses — contract §5.2.
#
# THE ONE THING TO UNDERSTAND HERE:
#
# Docker publishes ports by DNAT in iptables' PREROUTING chain. The destination
# address is rewritten to the container *before* the packet reaches INPUT,
# which is where the host firewall lives. So a firewall rule naming a
# Docker-published port is not wrong — it is never consulted. It will pass
# review, pass a grep of the ruleset, and constrain nothing.
#
# For anything Docker publishes, THE BIND ADDRESS IS THE ONLY ENFORCEMENT.
#
# Two rules follow from that, and they are the whole model:
#
#   1. No published port ever binds 0.0.0.0. Not even a service that is
#      supposed to be LAN-reachable. "Publish broadly, narrow with a firewall
#      rule" is precisely the mistake this section exists to prevent.
#   2. A service reachable from two places is published TWICE, on two explicit
#      addresses. Jellyseerr is the only service in that class.
#
# The host firewall is still real and load-bearing — but only for services
# Docker does not publish: SSH, and Jellyfin on host networking. There is
# deliberately no firewall rule for 5055 or any other published port, because
# an inert rule that reads as protection is worse than no rule at all.


# BIND_ADDR_TAILNET — Bazarr, Prowlarr, Radarr, SABnzbd, Sonarr, qBittorrent,
# and one of Jellyseerr's two bindings.
#
# Set to this machine's tailnet address: `tailscale ip -4`, e.g. 100.x.y.z.
#
# Defaults to 127.0.0.1 so an unconfigured stack fails closed. Until you set
# it, reach the UIs over an SSH tunnel:
#     ssh -L 9696:127.0.0.1:9696 palimpsest
#
# Ordering consequence: Docker cannot bind an address that does not exist yet,
# so the stack unit must wait for tailscale0 to have an IPv4 address. That is a
# start precondition in palimpsest-system, not just an After= (contract §3).
BIND_ADDR_TAILNET=127.0.0.1

# BIND_ADDR_LAN — Jellyseerr's second binding, and nothing else.
#
# Set to this machine's DHCP-reserved LAN address, e.g. 192.168.1.20. Jellyseerr
# is LAN-reachable on purpose (contract §5.1): it is the family-facing request
# UI, and confining it to the tailnet would put it out of reach of the people
# it exists for.
#
# THIS VARIABLE HAS NO DEFAULT, DELIBERATELY. Leaving it blank does not start
# a degraded stack — compose aborts immediately:
#
#     error while interpolating services.jellyseerr.ports.[]:
#     required variable BIND_ADDR_LAN is missing a value: must be set to this
#     machine's DHCP-reserved LAN address — contract §5.2, no default
#
# That is designed behaviour, not a bug: an unset LAN address must fail at
# bring-up, in front of you, rather than leave Jellyseerr listening somewhere
# nobody intended. It is enforced with compose's `${VAR:?message}` form because
# that is the only construct that actually aborts. Do not "fix" it by adding a
# default, and in particular do not rely on the two bindings colliding — they
# do not: compose silently deduplicates identical host_ip:port pairs and exits
# 0, which would leave Jellyseerr bound to loopback only, unreachable, with no
# error anywhere.
BIND_ADDR_LAN=


# ---------------------------------------------------------------------------
# DOCKER_SUBNET — the stack's bridge network.
#
# Pinned so gluetun's killswitch can be told which subnet to let replies out
# to. Change only if it collides with something on your LAN or tailnet; if you
# change it, this one value feeds both the network definition and gluetun.
DOCKER_SUBNET=172.28.0.0/16


# ---------------------------------------------------------------------------
# VPN — gluetun, wrapping qBittorrent only.
#
# Where these come from: your VPN provider's WireGuard config download. You
# generate or download a config file and copy the fields out of it; you do not
# use your account username/password for WireGuard.
#
# VPN_SERVICE_PROVIDER must be a name gluetun recognises — it is a fixed
# vocabulary, not free text. The list is at:
#   https://github.com/qdm12/gluetun-wiki/tree/main/setup/providers
# and includes mullvad, protonvpn, airvpn, ivpn, windscribe, nordvpn,
# privateinternetaccess, surfshark, and ~40 others. Use `custom` with
# VPN_ENDPOINT_IP/VPN_ENDPOINT_PORT if yours is not supported.
VPN_SERVICE_PROVIDER=
VPN_TYPE=wireguard

# WireGuard (VPN_TYPE=wireguard). From the provider's generated config:
#   [Interface] PrivateKey  -> WIREGUARD_PRIVATE_KEY
#   [Interface] Address     -> WIREGUARD_ADDRESSES   (e.g. 10.64.0.2/32)
#   [Peer]      PresharedKey-> WIREGUARD_PRESHARED_KEY   (blank if absent)
WIREGUARD_PRIVATE_KEY=
WIREGUARD_ADDRESSES=
WIREGUARD_PRESHARED_KEY=

# OpenVPN (VPN_TYPE=openvpn). Leave blank if using WireGuard. These ARE the
# provider account credentials, unlike the WireGuard fields above.
OPENVPN_USER=
OPENVPN_PASSWORD=

# Optional: restrict exit servers. Comma-separated, provider's own spelling,
# e.g. "Netherlands,Sweden". Blank means gluetun picks any server.
SERVER_COUNTRIES=
